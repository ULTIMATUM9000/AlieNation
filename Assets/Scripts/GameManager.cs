using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{

    public GameObject RespawnPoint;
    // Update is called once per frame
    void Update()
    {
        
    }

    public void PlayerStatusCheck()
    {
        //Logic: Check if Player hp == o
        //if so, respawn at SpawnPoint with max hp

        //if (GameObject.Find("Player").GetComponent<Player>.MaxHealth)
        //{

        //}

        //if kinulang sa time, gawin nalang nating immortal yung character
    }



}
